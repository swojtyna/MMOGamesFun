import XCTest
@testable import DashboardTabCoordinator

final class DashboardTabCoordinatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DashboardTabCoordinator().text, "Hello, World!")
    }
}
